
StructuredBuffer<float3>     a : register(t0);
StructuredBuffer<float3>     b : register(t1);
RWStructuredBuffer<float3>   c : register(u2);

[numthreads(64, 1, 1)]
void CSMain(int3 threadId: SV_DispatchThreadID)
{
    c[threadId.x] = a[threadId.x] + b[threadId.x];

}