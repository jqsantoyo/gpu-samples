{
    "tasks": [
        {
            "type": "shell",
            "label": "Clean",
            "command": "rm -rf .build/",
            "group": "build",
            "problemMatcher": [],
        }
        {
            "type": "shell",
            "label": "Format",
            "command": "clang-format -i ${file}",
            "group": "build",
            "problemMatcher": [],
        }
    ]
}